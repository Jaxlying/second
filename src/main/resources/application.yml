spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/second
    username: root
    password:
    test-while-idle: true
    validation-query: SELECT 1
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
multipart:
  maxFileSize: 12800KB
  maxRequestSize: 12800KB
server:
  port: 8080
yibanoauth:
  second:
    APPID: "d3e9e3722272dc94"
    APPkey: "202f365322d008bab2f76d5eea1bfb15"
  frontend: "http://localhost:4000"
  imgserver: "http://localhost:8080"